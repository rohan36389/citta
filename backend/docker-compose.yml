version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: citta-backend:latest
    container_name: citta-backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - ENVIRONMENT=development
      - DEBUG=true
    restart: unless-stopped
